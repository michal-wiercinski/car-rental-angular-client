import { ApiMessagePipe } from './api-message.pipe';

describe('ApiMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ApiMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
